# 🔐 Secret Key Generator (Python)

A simple command-line tool that generates a secure random secret key using Python.

The generated key can be used for API keys, configuration files, or other security-related purposes.

---

## 🛠 Technologies
- Python 3
- secrets (Python standard library)

---

## ▶ How to Run
1. Make sure Python 3 is installed.
2. Run the following command:

```bash
python secret_key.py
